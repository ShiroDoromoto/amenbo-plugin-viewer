# Copy to .dev.vars for `wrangler dev`. .dev.vars is gitignored — the real token never lands here.
#
# On the deployed Worker this is a Secret instead:
#
#   wrangler secret put AUTH_TOKEN
#
AUTH_TOKEN=a-long-random-token-for-local-development-only
